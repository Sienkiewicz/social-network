import React, { FC } from 'react'
import { AddNewPostForm } from './AddNewPostForm'

const PostArea: FC = () => {
  return (
    <div>
      <AddNewPostForm />
    </div>
  )
}

export default PostArea
